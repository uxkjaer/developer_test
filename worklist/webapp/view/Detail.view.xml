<mvc:View
	controllerName="ns.worklist.controller.Detail"
	xmlns="sap.m"
	xmlns:semantic="sap.f.semantic"
    xmlns:smartList="sap.ui.comp.smartlist"
	xmlns:mvc="sap.ui.core.mvc">

	<semantic:SemanticPage
		id="detailPage"
		busy="{detailView>/busy}"
		busyIndicatorDelay="{detailView>/delay}">
		<semantic:titleHeading>
			<Title
				text="{OrderID}"
				level="H2"/>
		</semantic:titleHeading>
		<semantic:headerContent>
	<!-- <ObjectAttribute title="{i18n>Customer}" text="{CustomerID}"/> -->
    <ObjectAttribute title="Sales Order" text="{OrderID}"/>
    <ObjectAttribute title="Company name" text="{Customer/CompanyName}"/>
  
    <ObjectAttribute title="Address" text="{Customer/Address}"/>
    <ObjectAttribute title="City" text="{Customer/City}"/>
        <ObjectAttribute title="Postalcode" text="{Customer/PostalCode}"/>
    <ObjectAttribute title="Country" text="{Customer/Country}"/>


</semantic:headerContent>
		<semantic:content>
			<smartList:SmartList id="smartProductList" smartFilter="smartFilterBar" listBindingPath="Order_Details" 
           expandFields="Product" header="Products List" showRowCount="true" showFullScreenButton="true" 
           enableAutoBinding="true">
           <smartList:listItemTemplate>
               <StandardListItem id="listTemplate" type="Navigation"   press="handleListItemPress" 
                                 title="{ProductID}" 
                                 info="{= ${UnitPrice} + ' €' }" description="{Product/ProductName}" />
           </smartList:listItemTemplate>
</smartList:SmartList>
		</semantic:content>
		<!--Semantic ShareMenu Buttons-->
		<semantic:sendEmailAction>
			<semantic:SendEmailAction
				id="shareEmail"
				press=".onSendEmailPress"/>
		</semantic:sendEmailAction>

		<semantic:closeAction>
			<semantic:CloseAction
					id="closeColumn"
					press=".onCloseDetailPress"/>
		</semantic:closeAction>

		<semantic:fullScreenAction>
			<semantic:FullScreenAction
					id="enterFullScreen"
					visible="{= !${device>/system/phone} &amp;&amp; !${appView>/actionButtonsInfo/midColumn/fullScreen}}"
					press=".toggleFullScreen"/>
		</semantic:fullScreenAction>
		<semantic:exitFullScreenAction>
			<semantic:ExitFullScreenAction
					id="exitFullScreen"
					visible="{= !${device>/system/phone} &amp;&amp; ${appView>/actionButtonsInfo/midColumn/fullScreen}}"
					press=".toggleFullScreen"/>
		</semantic:exitFullScreenAction>

	</semantic:SemanticPage>

</mvc:View>